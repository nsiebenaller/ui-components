import colors from "./colors.json";
import textColors from "./textColors.json";
import {
    colorOrDefault,
    standardizeColor,
    extractColorHue,
    extractColorValue,
    getHoverColor,
} from "./helpers";

export {
    colors,
    textColors,
    colorOrDefault,
    standardizeColor,
    extractColorHue,
    extractColorValue,
    getHoverColor,
};
